.claude/
.DS_Store
node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.env
.env.local
.env*.local

# Directus runtime data
# `database/data.db` + `uploads/` are committed so `docker compose up`
# restores the full state on a fresh clone. `extensions/` stays runtime-only.
extensions/

# Local WhatsApp media scratch folder
Tabernam/
